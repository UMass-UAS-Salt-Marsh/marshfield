% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dem_check.R
\name{dem_check}
\alias{dem_check}
\title{Compare a DEM with RTK plot heights, writing a point GeoPackage and a map}
\usage{
dem_check(
  dem,
  basemap = NULL,
  limit = 1.2,
  path = "C:/Work/saltmarsh/data/uas2026/field",
  ortho_path = "C:/Work/saltmarsh/data/uas2026/orthos",
  classes = "C:/Work/saltmarsh/pars/classes.txt"
)
}
\arguments{
\item{dem}{Name of the DEM in \code{orthos.txt} (e.g., 'Jul 20 low DEM')}

\item{basemap}{Name of the RGB ortho in \code{orthos.txt} to use as a basemap; default is the first
RGB ortho for the same site}

\item{limit}{Color scale runs from \code{-limit} to \code{limit} m; larger values are clamped}

\item{path}{Path to field data}

\item{ortho_path}{Path to orthoimages and DEMs}

\item{classes}{Path to classes table, used for subclass names}
}
\value{
sf point data frame of results, invisibly
}
\description{
Samples a DEM at each plot center and computes \code{dz} = DEM - RTK height (m). Positive \code{dz}
is expected where the DEM includes vegetation; spatially coherent patches of large
positive or negative \code{dz} suggest vertical warping in the DEM. Writes
}
\details{
\itemize{
\item \verb{prelim/dz_<dem>.gpkg}, a point layer in the plots' CRS (EPSG:6491) with \code{plot_id},
\code{date}, \code{rtk_id}, \code{section}, \code{subclass}, \code{subclass_name}, \code{rtk_height} (in the DEM's
vertical datum), \code{dem_height}, and \code{dz}.
\item \verb{prelim/dz_<dem>.png}, a map of \code{dz} over a faded orthophoto, with section labels.
}

The DEM and its vertical datum come from \code{pars/orthos.txt} (see \code{view_plots}). Plot centers
are put into the DEM's CRS as in \code{view_plots} (including the TEMPORARY datum shift).
}
